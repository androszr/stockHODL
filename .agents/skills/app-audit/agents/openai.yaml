interface:
  display_name: "App Audit"
  short_description: "Standalone audit of everything stock-follow's simulator test suite cannot see — entitlement drift between Debug and Rele"
  default_prompt: "Use $app-audit for this task."
