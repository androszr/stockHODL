# Direct dependencies are pinned to exact versions in package.json. This keeps
# it that way: `pnpm add <pkg>` writes "1.2.3", not "^1.2.3", so a dependency
# can only move when someone deliberately moves it.
save-exact=true
